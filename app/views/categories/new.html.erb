<% unless @category.errors.full_messages.nil? %>
  <% @category.errors.full_messages.each do |err|%>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg"><%= err%></p>
  <%end%>
<%end%>
<%= render "form", category: @category%>
